@model IEnumerable<Music.Models.Playlist>

@{
    ViewBag.Title = "Index";
}

<h2>Playlists</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Name, "AlbumsByPlaylist", new { id = item.PlaylistId })
            @*@Html.DisplayFor(modelItem => item.Name)*@
        </td>
        <td>
            @Html.ActionLink("Rename", "Edit", new { id=item.PlaylistId }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.PlaylistId }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.PlaylistId })
        </td>
    </tr>
}

</table>
